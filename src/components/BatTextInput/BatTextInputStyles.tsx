import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    width: "100%",
    backgroundColor: '#e5bf3c',
    color: '#000000',
    fontSize: 20,
    // height: 45,
    borderWidth: 1,
    borderRadius: 5,
    padding: 10,
    textAlign: 'center',
  }
});